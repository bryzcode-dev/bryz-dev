# Durable Knowledge
Store verified cross-project knowledge here as focused Markdown cards. Prefer one root cause, fix, pattern, dataset fact, or runbook per file. Do not store secrets or raw query results.
